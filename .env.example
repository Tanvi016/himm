# The secret PIN users enter to access the site
AUTH_PIN=your_pin

# Random string used to sign session cookies (use a long random value in production)
SESSION_SECRET=change-me-to-a-random-string

# Port for the Express server (Render sets this automatically)
PORT=3001
